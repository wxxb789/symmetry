# Goal 0006 progress receipt - 2026-09-14

## Subject

- Source commit: `87154bd6cca79d74139043db0ef62190e49a6a4e`
- Git tree: `2c886dd55284dcf99b243dd660c25bd7e88cbc93`
- Tracked manifest SHA-256: `e04cbebb3f160d30afd1963ce41a43c1ca8bcccd902f3ee19c5bd5b85b1c1577`

The manifest digest is SHA-256 over the UTF-8 output of
`git ls-tree -r --full-tree 87154bd6cca79d74139043db0ef62190e49a6a4e` joined
with LF. It binds this receipt to the tracked source subject, not to any
untracked evidence draft.

## Executed Checks

- `daemon`: `go test -count=1 -timeout 600s ./...` passed.
- `daemon`: `go vet ./...` passed.
- `control`: `mix format --check-formatted` passed.
- `control`: `mix compile --warnings-as-errors` passed.
- `control`: `mix test --no-color` passed: `742 passed`, `20 skipped`.
- Windows Pi `0.85.1`: `mix test test/symmetry_control/pi_control_e2e_test.exs --no-color`
  passed: `7 passed`, no exclusions. It covers ordinary repository work,
  crash recovery, candidate validation, fresh handoff, and retained resume.

The Pi module used the pinned test-only witness and a synthetic numeric IPv4
loopback upstream. It did not forward provider credentials.

## Verified Progress

- A relative Control lease is sampled after claim/renewal work, consumed by
  elapsed request time and safety margin, and armed with an immutable sequence.
  The first post-start watchdog renewal advances from sequence `1` to `2`.
- Windows containment keeps durable authority, receipt persistence, and release
  separate from ordinary process-handle cleanup. Callback mutation cannot alter
  the retained authority fence.
- Real Pi loopback exercises now cover durable receipts across completion,
  crash recovery, candidate validation, fresh handoff, and retained resume.
- CI has fail-closed artifact uploads for its durable native witness jobs and
  records the synthetic upstream boundary in Linux provenance.
- The Claude Code local transport smoke launches with an explicit child
  environment allowlist. It remains an opt-in transport-only check.

## Not a Completion Receipt

Goal 0006 remains incomplete. This receipt does not prove a credentialed
provider repository operation, hosted Windows CI execution, Windows OpenCode
native coverage, cross-machine recovery, or an authorized acceptance of a real
user Goal. The opt-in Claude smoke was not run against a real Claude executable
and loopback endpoint in this subject.

Untracked evidence drafts dated `2026-09-15` are in the future relative to this
receipt and are not evidence for this source subject. They remain unmodified
pending a separately reviewed consolidation.
